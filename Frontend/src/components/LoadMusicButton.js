import styles from "./LoadMusicButton.module.css";

const LoadMusicButton = props => {

    return <button className={styles.button} type="submit">load my music for today</button>
}

export default LoadMusicButton;